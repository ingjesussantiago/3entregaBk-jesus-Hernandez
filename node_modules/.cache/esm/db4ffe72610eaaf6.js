let fs;_610‍.x([["default",()=>managerProducto]]);_610‍.w("fs",[["default",["fs"],function(v){fs=v}]]);

               class managerProducto {

constructor(path){
    this.path = path
}

async getProducto(){
    if (fs.existsSync(this.path)) {
       const productos= await fs.promise.readFileSync(this.path, "utf-8")
       return JSON.parse (productos)
        
    } else {
        return []
    }
}



}